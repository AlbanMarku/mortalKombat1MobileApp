import { Text, View } from 'react-native';

export default function StringMoves({ stringAttacks, iconSet }) {
  console.log(stringAttacks[0].attackInputs);
  return <Text>I am strings screen</Text>;
}
